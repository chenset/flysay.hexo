<!DOCTYPE html>
<html>
<head>
    <%
    var title = page.title;

    if (is_archive()) {
        title = 'Archives';

        if (is_month()) {
            title += ': ' + page.year + '/' + page.month;
        } else if (is_year()) {
            title += ': ' + page.year;
        }
    } else if (is_category()) {
        title = 'Category: ' + page.category;
    } else if (is_tag()) {
        title = page.tag + ' - 标签';
    }
    %>
    <title>
        <% if (title){ %>
        <%= title %> -
        <% } %>
        <%= config.title %>
    </title>
    <meta charset="UTF-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>
    <%- css(
            '/css/bootstrap.min.css',
            '/js/highlightjs/styles/default.css',
            '/css/iconfont.css',
            '/css/base.css'
    ) %>

    <%- js(
            '/js/jquery-2.1.4.min.js'
    ) %>
    <% if(is_home()){ %>
    <link rel="canonical" href="<%= config.url %>"/>
    <% }else{ %>
    <link rel="canonical" href="<%= config.url + url_for(path) %>"/>
    <% } %>
    <!--<link href='http://fonts.lug.ustc.edu.cn/css?family=Lato&subset=latin,latin-ext' rel='stylesheet' type='text/css'>-->
</head>
<body>

<div id="pjax-container">
    <%- partial('_partial/header') %>
    <div id="main-container">
        <%- body %>
    </div>
    <%- partial('_partial/footer') %>
</div>

<div class="hvr-ripple-out scroll-to-top" id="scroll-to-top">
    <i class="icon iconfont icon-up"></i>
</div>
<%- js(
        '/js/jquery.pjax.min.js',
        '/js/highlightjs/highlight.pack.js',
        '/js/velocity.min.js',
        '/js/velocity.ui.min.js',
        '/js/base.js'
) %>
</body>
</html>
